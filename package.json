{
  "name": "particles-spotinst",
  "version": "1.0.2",
  "publishConfig": {
    "tags": "latest"
  },
  "description": "Condensation particles for building AWS CloudFormation templates.",
  "scripts": {
    "test": "./node_modules/.bin/mocha",
    "build": "./node_modules/.bin/gulp condensation:build",
    "deploy": "./node_modules/.bin/gulp condensation:deploy",
    "gulp": "./node_modules/.bin/gulp"
  },
  "license": "Apache-2.0",
  "keywords": [
    "condensation-particles",
    "aws",
    "cloudformation",
    "cloud",
    "formation"
  ],
  "dependencies": {
    "condensation": "^0.5.2",
    "config": "^1.20.0",
    "git-rev-sync": "^1.6.0",
    "gulp": "^3.9.1",
    "particles-core": "^0.4.5"
  },
  "devDependencies": {
    "condensation-particle-tests": "^0.5.0",
    "mocha": "^2.4.5"
  }
}
